import tabs from"../../components/Tabs.js";import Form from"../../components/Form.js";import nav from"../../components/Navigation.js";import user from"../../model/web/User.js";function fnAdmin(){const e=new Form("#profile");e.submit(n=>{e.isValid(user.validateProfile)&&e.send().then(n=>{tabs.setActive(0),e.setOk(n)}),n.preventDefault()}),nav.setScript("admin-js",fnAdmin)}nav.ready(fnAdmin);export default fnAdmin;